package top.kwseeker.antlr4.mysql.sharding.parser.mysql.visitor.statement;

//自动生成的 MySQLStatementBaseVisitor 定义了针对 ANTLR4 中定义的每个规则的默认访问方法实现：
//默认是深度优先遍历，访问每一个规则节点, 但是并不会做什么

//这里的业务是我们需要改写对我们关心的规则的访问逻辑，通过遍历解析树（ParseTree）后将解析树转成 SQLStatement（AST）